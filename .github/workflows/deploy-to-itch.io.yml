name: Publish on Itch.IO  🎮
on: [workflow_call, workflow_dispatch]

jobs:
  release_windows:
    runs-on: ubuntu-latest
    steps:
       - name: Checkout code
         uses: actions/checkout@v3

       - uses: actions/download-artifact@v3
         with:
           name: build-WebGL
           path: build/WebGL
           
       - name: list-files
         run: ls -R
         working-directory: build/WebG

       - name: using-api-josephbmanley
         uses: josephbmanley/butler-publish-itchio-action@v1.0.3
         
    env:
      GITHUB_TOKEN: "${{ secrets.GH_CREDENTIALS }}"
      BUTLER_CREDENTIALS: ${{ secrets.ITCHIO_KEY }}
      CHANNEL: HTML
      ITCH_GAME: kurzgesagt
      ITCH_USER: spark8
      PACKAGE: build/WebG/build-WebGL
      VERSION: 1 #${{ steps.downloading.outputs.version }}


          
      
